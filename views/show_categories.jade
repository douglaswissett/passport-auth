extends layout

block content
  .container
    .row
      .col-md-6
        ul
          each category in categories
            li 
              a(href="/api/v1/categories/" + category.name) #{category.name}
